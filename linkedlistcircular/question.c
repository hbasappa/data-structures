/**
 * problem:
 *      find the mid-point of a singly linked list
 * examples:
 *      1->2->3->4->5 => FALSE
 *      1->2->3->4->5->1 => TRUE (assuming node 1 address is same as in the beg))
 * input: 
 *      list head pointer
 * output:
 *      bool: TRUE for circular;FALSE otherwise)
 * complexity:
 *      time = O(N)
 *      space = O(1)
**/

#include "test.h"

/*
typdef struct Node {
    int val;
    struct Node *next;
} Node;
*/

bool listCircular(Node *head) {
    /* your code here */
	return true;
}
