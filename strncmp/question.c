/**
 * problem:
 *      implement strncmp() in C/C++ to returns < 0 
 *      if str1 is less than str2; > 0 if str1 is greater than str2, and 0 if they are equal.
 * examples:
 *       abcd, abcd, 4 => 0
 *       abc, abcd, 3 => 0
 *       abc, abcd, 4 => < 0
 *       abcdef, abcd, 4 => > 0
 * complexity:
 *      time = 
 *      space =
**/

#include "test.h"

int my_strncmp(const char *s1, const char *s2, int n) {
    /* your code here */

	return 0;
}
